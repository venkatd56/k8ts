#labels and Annotations

Labels provide addional metdata information to the pod in the form of key value pairs, which helps to filter and selecting/quering the objects.

Annotations provides more additional information that cannot be queried or selected more generic way we call it as documentation which gives more info regarding the object